VITE_API_URL=<backend-url>
# Required for camera/microphone access from another device on your LAN.
# Use a certificate trusted by that device (for example, one created with mkcert).
# VITE_TLS_KEY_PATH=../backend/certs/teamora-lan-key.pem
# VITE_TLS_CERT_PATH=../backend/certs/teamora-lan.pem
